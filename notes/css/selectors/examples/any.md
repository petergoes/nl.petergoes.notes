The `*` selector matches any `<element>` on the page

```css
* {
    border: 1px solid black;
    padding: 1rem;
}
```

```html
<div>
    <section>
        A nested section element
    </section>
</div>
<p>A p element</p>
```
